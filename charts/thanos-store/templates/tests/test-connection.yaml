apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thanos-store.fullname" . }}-test-connection"
  labels:
    {{- include "thanos-store.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thanos-store.fullname" . }}:{{ .Values.service.httpPort }}/-/ready']
  restartPolicy: Never
