{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "thanos-store.roleBindingName" . }}
  labels:
    {{- include "thanos-store.labels" . | nindent 4 }}
  {{- with .Values.rbac.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "thanos-store.roleName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "thanos-store.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
