{{- if .Values.servers.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pgadmin.fullname" . }}-servers
  labels:
    {{- include "pgadmin.labels" . | nindent 4 }}
data:
  servers.json: |
    {{- include "pgadmin.serversJson" . | nindent 4 }}
{{- end }}
