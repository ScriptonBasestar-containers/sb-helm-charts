apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pgadmin.fullname" . }}
  labels:
    {{- include "pgadmin.labels" . | nindent 4 }}
type: Opaque
data:
  password: {{ .Values.pgadmin.defaultPassword | b64enc | quote }}
