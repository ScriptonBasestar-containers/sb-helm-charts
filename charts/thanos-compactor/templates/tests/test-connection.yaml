apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thanos-compactor.fullname" . }}-test-connection"
  labels:
    {{- include "thanos-compactor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "thanos-compactor.fullname" . }}:{{ .Values.service.httpPort }}/-/ready']
  restartPolicy: Never
