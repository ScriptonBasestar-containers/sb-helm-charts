apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loki.fullname" . }}-test-connection"
  labels:
    {{- include "loki.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "loki.fullname" . }}:{{ .Values.service.port }}/ready']
  restartPolicy: Never
