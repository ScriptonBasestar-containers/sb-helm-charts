apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "blackbox-exporter.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "blackbox-exporter.labels" . | nindent 4 }}
data:
  blackbox.yml: |
    modules:
      {{- toYaml .Values.config.modules | nindent 6 }}
