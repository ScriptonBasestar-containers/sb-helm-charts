apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube-state-metrics.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kube-state-metrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  containers:
    - name: wget
      image: busybox
      command: ['sh', '-c']
      args: ['wget --spider -S http://{{ include "kube-state-metrics.fullname" . }}:{{ .Values.service.port }}/healthz 2>&1 | grep "HTTP/" | awk "{print \\$2}"']
  restartPolicy: Never
